{% extends 'base.html.twig' %}

{% block title %}{{ movie.title }}{% endblock %}

{% block body %}

<h1>{{ movie.title }}</h1>

<h2>Casting</h2>
<ul>
    {% for casting in movie.castings %}
    <li>{{ casting.person.name }} as {{ casting.role }}</li>
    {% endfor %}
</ul>

{% endblock %}